package attributes;

/**
 * PLANS:
 *
 * make this interface an "Attribute"
 * Change the attribute class to something like "Bonus"
 * Make an interface called "BonusContainer" that implements the use of getBonuses (that returns a hashtable of all attributes and their total bonuses
 *
 */
public interface Attributable {
    String toID();
}
